# Core
numpy>=1.21
scipy>=1.8
pandas>=1.3

# Visualization / UI
matplotlib>=3.4
ipywidgets
notebook>=6.5
ipython>=8.0

# Image Processing
scikit-image
imageio
tifffile
Pillow

# Metrics
# Note: psnr-hvsm binaries are available for x86_64. 
# For ARM (M1/M2 Mac), you may need to build from source or skip this metric.
psnr-hvsm; platform_machine == "x86_64" or platform_machine == "AMD64" or platform_machine == "i386"